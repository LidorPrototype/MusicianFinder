<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.LYEO.musicianfinder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Posts.DisplayPosts"></activity>
        <activity android:name=".Posts.SearchScreenActivity" />
        <activity android:name=".Chat.Chat" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Chat.Rooms" />
        <activity android:name=".Publish_Musician" />
        <activity android:name=".Publish_Band" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".Posts.PublishYourselfActivity" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".BandSearchScreenActivity" />
        <activity android:name=".PrivateAreaActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".WelcomePageActivity" />
        <activity android:name=".EntrancePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>